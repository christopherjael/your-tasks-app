/* window.addEventListener('load', () => {
  const $form = document.forms[0];

  $form.addEventListener('submit', () => {
    let $inputTitle = $form.elements[0];
    let $inputDescription = $form.elements[1];

    $inputTitle.value = '';
    $inputDescription.value = '';
  });
}); */
